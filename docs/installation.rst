============
Installation
============

At the command line::

    $ easy_install django-usda-mongo

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-usda-mongo
    $ pip install django-usda-mongo
